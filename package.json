{
  "name": "electron-game-overlay",
  "type": "module",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev:main": "tsc -p main -w & cross-env ELECTRON_START_URL=http://localhost:5173 electron .",
    "build:main": "tsc -p main",
    "start": "cross-env ELECTRON_START_URL=http://localhost:5173 electron .",
    "dev": "npm-run-all -p dev:renderer dev:main"
  },
  "devDependencies": {
    "@types/node": "^20",
    "electron": "^31",
    "typescript": "^5.5",
    "cross-env": "^7",
    "npm-run-all": "^4.1.5"
  }
}
